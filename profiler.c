#include <stdio.h>
#include <stdbool.h>

int main() {
	printf("hello,");
	bool b = true;
	// use an if/else to check if latest
	// vim indentation settings are working
	if (b)
		printf(" world\n");
	else
		printf("\n");
    return 0;
}
